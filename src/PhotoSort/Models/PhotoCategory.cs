namespace PhotoSort.Models;

/// <summary>Where a photo currently lives relative to the chosen root folder.</summary>
public enum PhotoCategory
{
    None,
    Edit,
    Archive,
    Delete
}
